"""
Python中的数据容器
"""

"""
数据容器是一种可以容纳多份数据的数据类型，
数据容器可以容纳多份数据，
容纳的每一份数据称为“一个元素”，
每一个元素可以是任意类型的数据，
如：字符串，布尔，数字，等等
"""

"""
根据容器特点的不同，
如：是否支持重复元素，是否可以修改，是否有序，等等
分为5类：列表（list），元组（tuple），字符串（str），集合（set），字典（dict）
"""